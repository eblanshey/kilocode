---
"kilo-code": minor
---

Render inline LaTeX between single dollar signs (for example `$E = mc^2$`) in chat. Currency like $93K, price ranges like $5-$8, and shell variables like $PATH are still shown as plain text.
